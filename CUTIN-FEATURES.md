# 신규 서비스 상세 기능 명세서

> **문서 버전:** v0.3 (2026-08-12 — iOS 네이티브 전환 확정: 스파이크 검증 통과, §0.1)
> **작성 목적:** 러프 명세 기반 상세 기능 정의, 개발 착수용
> **확정 스택:** 클라이언트는 **iOS 네이티브 (SwiftUI, iOS 26 타깃)**, 어드민은 Next.js(TypeScript), 서버는 Node(NestJS) 또는 동등 수준
> **상태 표기:** 🟢 확정 / 🟡 가정·검토필요 / 🔴 미정·결정필요
>
> **주의 — 표기는 "결정 상태"이고 "구현 상태"가 아니다.** 🟢은 무엇을 만들지 정해졌다는 뜻일
> 뿐, 그 기능이 앱에 들어갔다는 뜻이 아니다. 0.1.0에 실제로 들어간 범위는 `README.md`의
> "릴리즈 0.1.0 범위"를 본다. 예를 들어 §6.3(대표 컷 지정)은 🟢이지만 0.1.0에는 없다.

---

## 0. 서비스 개요

사진/영상 컷을 촬영해 템플릿으로 가공·업로드하고, 친구들과 포스트를 공유·반응하는 소셜 기록 서비스. 일정 압박이 있는 MVP이므로, 본 명세는 **"무엇을 만들지"를 확정**하고 결정이 필요한 지점을 명시적으로 분리하는 데 목적이 있다.

서비스는 두 개로 구성된다.

- **메인 서비스(클라이언트):** 일반 사용자용 앱
- **어드민 서비스:** 운영/모더레이션용 (러프 명세에 항목이 없어 표준 구성으로 가정 — §10)

### 0.1 플랫폼·저장소 구성 🟢

클라이언트는 React Native(Expo) 단일 코드베이스로 착수했으나 **iOS 네이티브(SwiftUI) 단일 앱으로 전환을 확정**했다. 근거는 세 가지다.

1. Android를 실제로 빌드·검증한 적이 없어 크로스 플랫폼의 이득은 못 받으면서 RN 빌드 비용은 그대로 치르고 있다.
2. 디자인 정체성인 iOS 26 Liquid Glass가 Android에서 단색 폴백되며, 대체 표현이 준비되어 있지 않다.
3. 제품의 코어 루프인 촬영·이미지 합성(§5·§6)이 네이티브 카메라·이미지 파이프라인의 이득이 가장 큰 영역이다.

> ✅ **전환 확정 (2026-08-12).** 촬영 → 필터 → N컷 합성 → 저장 → 피드 표시 수직 슬라이스가 SwiftUI로 실기기에서 검증되었다. `cutin-ios`가 메인 클라이언트이며, MVP 1차(release/0.1.0)를 로컬 전용으로 진행하고 서버 연동은 0.2.0에서 붙인다. RN 앱 코드 제거는 별도 작업으로 미룬다.

| 저장소           | 역할                                                     |
| ---------------- | -------------------------------------------------------- |
| `cutin-ios`      | 메인 서비스 클라이언트 (SwiftUI)                         |
| `cutin-frontend` | 어드민 서비스 (Next.js) + 기존 RN 앱 (전환 확정 시 제거) |
| `cutin-backend`  | API 서버                                                 |

**Android는 무기한 연기한다.** 1인 개발 체제에서 네이티브 2벌은 유지할 수 없으며, 수요가 확인되면 별도 저장소(`cutin-android`)로 재검토한다. 이에 따라 본 명세의 클라이언트 요구사항은 **iOS 기준**으로 읽는다.

---

## 1. 검토 의견 및 결정 필요 사항 (먼저 읽을 것)

명세를 그대로 구현하면 막히는 지점들이다. MVP 일정을 지키려면 아래는 **착수 전에 결정**해야 한다.

| #   | 이슈                                                                                                                                                                   | 영향             | 권장안                                                           |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ---------------------------------------------------------------- |
| 1   | **🔴 "이전 포스트 미완료 시 신규 생성 차단"** — 동시 작성 중인 draft를 1개로 제한할지, 임시저장 후 이어쓰기를 줄지                                                     | 촬영 플로우 전체 | **확정**: draft 1개 제한 + 이어쓰기/폐기 선택 + 24h 만료 (§5.3) |
| 2   | **🔴 친구목록 로드(인스타/카카오/X), 구글 제외** — 각 플랫폼의 친구·팔로워 API 접근은 정책상 대부분 제한됨 (인스타 Graph API는 팔로워 목록 미제공, X는 유료·승인 필요) | 온보딩 핵심 기능 | **확정:** 구글로 진입한 경우 친구 추가 단계 건너뛰기             |
| 3   | **🟡 OAuth 4종** — 인스타는 로그인용(Basic Display/Business)과 친구 import가 별개. 4종 모두 심사·키 발급 리드타임 있음                                                 | 로그인 전체      | **확정:** MVP는 카카오+구글 우선, 인스타/X는 2차                 |
| 4   | **🟡 영상 처리** — 촬영에 영상 포함 시 인코딩/스토리지/썸네일 추출 파이프라인 필요 (사진 대비 난이도 급상승)                                                           | 촬영·업로드      | **확정:** MVP는 사진 우선, 영상은 길이 제한(예: 15s) 두고 단계적 |
| 5   | **🟡 알림 선호 시간대(4개 슬롯)** — 푸시 발송 스케줄러·타임존 처리 필요                                                                                                | 알림             | **확정:** 3-3 슬롯, 기본값 아침                                  |
| 6   | **🔴 포스트 "남/본인" 노출 범위** — 전체공개/친구공개/비공개 정책 미정                                                                                                 | 피드·권한 전반   | **확정:** 포스트는 친구공개, 계정은 전체공개                     |
| 7   | **🟡 데이터 보존/삭제, 신고 처리** — 어드민·법적 요구. 명세에 없음                                                                                                     | 운영             | **확정:** 삭제 데이터 1년 보존                                   |

---

## 2. 인증 (로그인)

### 2.1 OAuth 로그인 🟡

- 지원 프로바이더: **카카오톡, 구글**(MVP) / **인스타그램, X**(2차)
- 신규 사용자는 로그인 성공 후 **온보딩 미완료 상태**로 분기 → §3
- 기존 사용자는 메인 피드로 진입

### 2.2 세션·토큰

- Access / Refresh 토큰 기반, Refresh는 httpOnly 보관(웹) / Secure Storage(앱)
- 동일 이메일의 다른 프로바이더 가입 시 **계정 연결(linking)** 정책 필요 🔴 (MVP: 이메일 기준 자동 연결 or 별도 계정 — 결정 요)

### 2.3 예외/엣지

- 프로바이더 동의 거부, 토큰 만료, 네트워크 실패 → 재시도 UI
- 탈퇴 후 재가입 시 동일 식별자 처리

---

## 3. 온보딩

로그인 직후 신규 사용자 1회 수행. **각 단계는 건너뛰기 가능 여부를 명시**해야 한다.

### 3.1 닉네임 🟢

- 중복 검사(실시간), 길이/금칙어 검증, 변경은 추후 프로필에서

### 3.2 친구목록 로드 (구글 제외) 🔴

- **실현성 사전 확인 필수** (§1-2 참조)
- 대체 설계 권장: 연락처 매칭(동의 기반) + 초대 링크
- 건너뛰기 허용, 추후 §9에서 추가 가능

### 3.3 알림 선호 시간대 선택 (4개 슬롯) 🟡

- 슬롯 예시(확정 필요): 아침(08–10) / 점심(12–14) / 저녁(18–20) / 밤(21–23)
- 다중 선택 허용 여부 결정 요. 디바이스 타임존 기준 발송
- 미선택 시 기본값 적용

### 3.4 프로필 사진 등록 🟢

- 업로드/크롭, 건너뛰기 시 기본 아바타

### 3.5 서비스 팁 화면 🟢

- 캐러셀 형태 안내, 마지막에 "시작하기" → 메인 진입
- 다시 보지 않기 + 도움말에서 재열람 가능

---

## 4. 메인 (홈)

### 4.1 피드 — 무한 스크롤 🟢

- **남/본인 포스트 목록**을 `infinite query`(cursor 기반 페이지네이션)로 로드
- 정렬: 최신순(MVP). 노출 범위는 §1-6 정책 의존
- 빈 상태(친구 없음/포스트 없음) UI 필요
- 당겨서 새로고침, 로딩 스켈레톤, 에러 재시도

### 4.2 네비게이션 바 (5탭) 🟢

1. **홈** — 피드(4.1)
2. **친구목록** — §9
3. **촬영 버튼** — §5 (중앙 강조 CTA)
4. **프로필** — §8
5. **기록 보관** — §6/§7 통합 진입점이 아닌 별도 보관함 → 아래 주의

> ⚠️ 러프 명세 상 "기록 보관"이 네비 항목(2-5)과 최상위 항목(4)에 중복 등장한다. **동일 화면으로 통합**한다고 가정. 🟡

### 4.3 알림 🟢

- 인앱 알림 목록(반응/댓글/친구요청/시스템)
- 읽음 처리, 미읽음 뱃지
- 푸시 알림과 §3.3 시간대 연동

---

## 5. 촬영

### 5.1 컷 수 선택 🟢

- **1컷 / 2컷 / 4컷 / 6컷** (확정 — §13. RN판·iOS판 구현이 같은 값을 쓴다)

### 5.2 촬영 방식 선택 🟢

- **(A) 한 번에 N컷 연속 촬영** / **(B) 한 장씩 개별 촬영**

### 5.3 미완료 포스트 처리 🟢 (핵심 정책 — 확정)

- (B) 방식에서 N컷을 다 채우지 않고 이탈하면 **draft 상태**로 보존
- 신규 촬영 진입 시 차단 메시지: _"이전 포스트 완료 후 생성할 수 있어요"_
- **확정:** 동시 draft 1개 제한. 진입 시 [이어서 작성] / [폐기하고 새로 시작] 선택 제공
- **확정:** draft는 생성 후 **24h 경과 시 만료**. 만료 판정은 백그라운드 삭제가 아니라
  **읽는 시점 검사** — 로컬 전용 단계에서 앱이 꺼져 있는 동안 돌 스케줄러가 없기 때문

### 5.4 영상 / 사진 선택 🟡

- MVP는 사진 우선(§1-4). 영상 도입 시: 최대 길이, 포맷, 인코딩, 썸네일 자동 추출 정의 필요
- 카메라/마이크/저장소 권한 처리, 거부 시 안내

---

## 6. 촬영 이후 (편집·업로드)

### 6.1 템플릿 선택 🟢

- 컷 수에 맞는 레이아웃 템플릿 목록. 템플릿 메타데이터(슬롯 수, 비율) 정의 필요

### 6.2 미리보기 🟢

- 적용 결과 실시간 렌더, 편집 단계 되돌아가기

### 6.3 썸네일 지정 🟢

- N컷 중 대표 컷 지정(미지정 시 첫 컷 기본)

### 6.4 업로드 🟢

- 공개 범위 설정(§1-6), 캡션(선택), 업로드 진행률/실패 재시도
- 업로드 완료 시 draft 해제

---

## 7. 포스트 상세

### 7.1 공유하기 🟢

- 외부 공유(딥링크/웹뷰), 비공개 포스트 공유 차단

### 7.2 댓글 🟢

- 작성/삭제, 페이지네이션, 신고. 대댓글 지원 여부 결정 요 🟡

### 7.3 반응 (이모지 등) 🟢

- 반응 종류 정의, 토글, 카운트 집계, 본인 반응 표시

### 7.4 보관 (북마크) 🟢

§4.2-5 "기록 보관" 탭의 데이터 원본. 러프 명세에 항목이 없어 아래로 정의한다.

- 포스트 단위 토글. 보관 목록은 최신 보관순
- **보관 여부는 포스트 레코드의 필드가 아니라 별도 목록**으로 둔다 — 0.2.0에서 이 목록이
  서버 상태로 바뀔 때 포스트 스키마를 건드리지 않기 위해
- 0.1.0은 내 포스트만 존재하므로 "남의 포스트 보관" 케이스는 발생하지 않는다

### 7.5 삭제 🟡

- 0.1.0(로컬 전용)은 **즉시 파일 삭제**. 되돌리기·휴지통 없음
- §1-7의 "삭제 데이터 1년 보존"은 서버 데이터 보존 정책이라 로컬 단계에 적용할 대상이 없다.
  soft delete 여부는 서버가 붙는 0.2.0에서 결정한다 🟡

---

## 8. 프로필

### 8.1 본인 프로필 🟢

- 프로필 사진, 닉네임, 내 포스트 그리드, 설정 진입

### 8.2 타인 프로필 🟡

- 친구 추가/요청, 차단·신고, 포스트 노출은 공개범위 의존

### 8.3 설정

- 알림(§3.3 재설정), 계정/로그아웃/탈퇴, 약관/개인정보

---

## 9. 친구 목록

### 9.1 추천 친구 🟡

- 추천 로직 정의 필요(연락처 매칭/공통 친구/신규). MVP는 단순 규칙

### 9.2 검색 🟢

- 닉네임/식별자 검색, 친구 요청·수락·삭제 상태머신
- 친구 관계 모델: 양방향(상호수락) vs 단방향(팔로우) 결정 요 🔴

---

## 10. 어드민 서비스 🟡 (가정 — 검토 요망)

러프 명세에 항목이 없어 운영에 필요한 표준 구성으로 가정한다.

- **인증·권한(RBAC):** 어드민/모더레이터 역할 분리
- **사용자 관리:** 조회/검색, 정지·해제, 탈퇴 처리
- **콘텐츠 모더레이션:** 신고 큐(포스트/댓글/프로필), 숨김·삭제, 처리 이력
- **신고 처리:** 사유 분류, 상태(접수/처리중/완료), 대응 로그
- **공지/푸시:** 전체·세그먼트 발송, 예약 발송
- **통계 대시보드:** DAU/MAU, 가입·게시·반응 추이
- **감사 로그:** 어드민 행위 추적(법적·보안)

---

## 11. 공통 비기능 요구사항

- **에러/빈/로딩 상태**를 모든 목록·폼 화면에 정의(표준 컴포넌트화)
- **권한 처리:** 카메라/마이크/연락처/알림 — 거부 시 폴백
- **이미지/영상 스토리지:** 객체 스토리지 + CDN, 업로드 서명 URL
- **개인정보·약관:** 동의 흐름, 데이터 삭제 요청 처리
- **분석 이벤트:** 핵심 퍼널(가입→온보딩→첫 촬영→첫 업로드) 트래킹
- **접근성/i18n:** 최소 타임존 처리(알림), 추후 다국어 여지

---

## 12. MVP 범위 제안 (일정 우선)

| 포함 (Phase 1)                  | 후행 (Phase 2+)              |
| ------------------------------- | ---------------------------- |
| 카카오·구글 로그인              | 인스타·X 로그인, 친구 import |
| 온보딩(닉네임/사진/팁/알림슬롯) | 플랫폼 친구목록 로드         |
| 사진 촬영(1·N컷, draft 1개)     | 영상 촬영·인코딩             |
| 템플릿·미리보기·업로드          | 고급 편집                    |
| 피드(무한스크롤)·상세·댓글·반응 | 대댓글, 고급 추천            |
| 친구 검색·요청, 기본 추천       | 알고리즘 추천                |
| 어드민: 신고·사용자관리 기본    | 통계·세그먼트 푸시           |

---

## 13. 결정 대기 항목 체크리스트 (🔴 우선)

- [x] 미완료 포스트 정책 (draft 제한·만료) — §5.3 (확정: 동시 1개 + 이어쓰기/폐기, 24h 만료)
- [ ] 플랫폼 친구목록 import 실현성 — §3.2 / §1-2
- [ ] 포스트 공개 범위 정책 — §1-6
- [ ] 친구 관계 모델(상호/단방향) — §9.2
- [x] 컷 수 옵션 값 — §5.1 (확정: 1·2·4·6 — RN판·iOS판 공통 구현)
- [ ] 멀티 프로바이더 계정 연결 정책 — §2.2
- [ ] 알림 슬롯 정의·다중선택 — §3.3
